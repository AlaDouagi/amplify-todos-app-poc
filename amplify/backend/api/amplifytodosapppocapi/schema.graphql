type Todo @model {
  id: ID!
  title: String!
  description: String!
  updatedAt: String!
  createdAt: String!
}

type Query {
  getMatchingTodos(title: String!): [Todo] @function(name: "matchtodos-${env}")
}
